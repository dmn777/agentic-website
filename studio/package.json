{
  "name": "agentic-website-studio",
  "private": true,
  "version": "1.0.0",
  "description": "Sanity Studio for the Notes section of Unattended (hosted on *.sanity.studio). Schemas live in code.",
  "type": "module",
  "scripts": {
    "dev": "sanity dev",
    "build": "sanity build",
    "deploy": "sanity deploy",
    "schema:deploy": "sanity schema deploy"
  },
  "dependencies": {
    "react": "^19.2.2",
    "react-dom": "^19.2.2",
    "sanity": "6.16.0",
    "styled-components": "^6.1.15"
  }
}
